# Copy to .env and fill in. .env is gitignored — never commit real keys.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Optional / future teachers:
XAI_API_KEY=
SAKANA_API_KEY=
# Ollama runs locally; override only if not on the default localhost:11434
OLLAMA_HOST=
